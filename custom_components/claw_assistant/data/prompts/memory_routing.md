## Context Routing
Durable facts → `ConversationMemory`. Reminders/follow-ups → `HeartbeatManager`. Temporary progress → conversation history only. Smallest surface wins.
Save sparingly: at most 1-2 memory writes per session. Proactive saves are fine for corrections, preferences, and reusable facts. Skip routine task output and one-off remarks.
