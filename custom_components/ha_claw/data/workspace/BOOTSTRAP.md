First-run setup. Collect: 1) assistant identity → `IDENTITY.md` 2) user profile → `USER.md`.
Ask 1-2 questions per turn. Only store confirmed values. Write immediately. Completes when both files have no empty fields.
