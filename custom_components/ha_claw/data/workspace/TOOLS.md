Environment-specific notes only.

Examples:

- Cameras: `<camera-slug>`
- Speakers: `<speaker-slug>`
- Notifications: `<notify-target>`

Long-lived access token (optional):
If the AI cannot read logs or run shell commands, add your HA token here.
Create one at: HA Profile → Long-Lived Access Tokens (bottom of page).

- ha_token: `<your-token>`
