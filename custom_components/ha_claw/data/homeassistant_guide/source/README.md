# Home Assistant Guide Source Layer

This folder keeps the migrated source material behind the runtime guide.

Use it only when the runtime documents are not enough.

Read order:

1. `../runtime/*.md`
2. `SKILL.md`
3. `references/*.md`

Purpose:

- `runtime/`: primary playbooks for claw_assistant
- `SKILL.md`: source-layer summary
- `references/`: deep reference material
